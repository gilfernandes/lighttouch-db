/**
 * The basis for the window that opens the form definition in JSON.
 */
isc.defineClass("RefreshButton", "IButton").addProperties({
    title: msgRefresh,
    icon: "[SKIN]/pickers/refresh_picker.png"
});