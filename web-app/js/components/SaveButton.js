/**
 * The basis for the window that opens the form definition in JSON.
 */
isc.defineClass("SaveButton", "IButton").addProperties({
    title: msgSave,
    icon: "[SKIN]/headerIcons/save_Over.png"
});